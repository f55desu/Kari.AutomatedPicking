# SQL_CATALOG — Справочник SQL-хранилища

> **Сервер**: `cl01sql` (Windows Auth, ODBC Driver 17 for SQL Server)  
> **Назначение**: Общий справочник всех известных таблиц/view для использования во всех проектах  
> **Обновлять**: при обнаружении новых таблиц или уточнении формул

---

## Базы данных

| База | Назначение |
|------|-----------|
| `DBReport` | Операционные данные маркетплейсов (продажи, заказы, остатки, артикулы) |
| `DBPartners` | Партнёрские данные (остатки МП, себестоимость, склады Кари) |

---

## DBReport

### mp.wb_sales_report
**Назначение**: Транзакции WB — продажи, возвраты, логистика, хранение  
**Гранулярность**: Строка = одна транзакция/документ  
**Временной диапазон**: ~622+ дней  

| Поле | Тип | Описание |
|------|-----|----------|
| `[Артикул поставщика]` | varchar | vendorCode (артикул Кари, UPPER для надёжности) |
| `[Тип документа]` | nvarchar | `Продажа`, `Возврат`, `(blank)` |
| `[Дата продажи]` | date | Дата транзакции |
| `[Кол-во]` | int | Количество (знак не инвертирован для возвратов!) |
| `[Цена розничная]` | float | Цена продажи клиенту (GMV per unit) |
| `[Цена розничная с учетом согласованной скидки]` | float | Цена со скидкой (для расчёта комиссии) |
| `[Размер кВВ, %]` | float | Процент комиссии WB |
| `[Вайлдберриз реализовал Товар (Пр)]` | float | WB realization amount |
| `[Услуги по доставке товара покупателю]` | float | Логистика (с НДС 22%) |
| `[Хранение]` | float | Хранение (с НДС 22%) |
| `[Платная приемка]` | float | Платная приёмка (с НДС 22%) |
| `[Возмещение издержек по эквайрингу]` | float | Эквайринг (с НДС 22%) |
| `[Обоснование]` | nvarchar | Подтип для blank-документов: `Логистика`, `Удержание`, `Штраф` |

**Типы документа:**
| Тип | Строк (пример) | Подтипы |
|-----|---------------|---------|
| `Продажа` | ~34K | Продажа, Возмещение ПВЗ, Компенсация скидки |
| `Возврат` | ~1.8K | Возврат, Компенсация ущерба |
| `(blank)` | ~240K | Возмещение перевозки (237K), Логистика, Удержание, Штраф |

**Ключевые формулы (подтверждены сверкой с QV, Δ<0.5%):**
```sql
-- Кол-во (net)
SUM(CASE WHEN [Тип документа]=N'Продажа' THEN [Кол-во]
         WHEN [Тип документа]=N'Возврат' THEN -[Кол-во] ELSE 0 END)

-- Выручка GMV
SUM(CASE WHEN ... THEN [Цена розничная] WHEN ... THEN -[Цена розничная] ... END)

-- Средняя цена дня (для эластичности)
SUM([Цена розничная]) / NULLIF(SUM([Кол-во]), 0)  -- WHERE [Тип документа]=N'Продажа'

-- Комиссия с НДС (точная, Δ=0.00%)
SUM(([Цена розничная с учетом согласованной скидки] / 1.2) * ([Размер кВВ, %] / 100.0) * 1.2)
```

> ⚠️ **Не совпадают с QV**: Логистика, Удержания, Продвижение — использовать с осторожностью

---

### mp.wb_sales_funnel_lk
**Назначение**: Воронка WB — заказы, выкупы, конверсия per артикул per день  
**Гранулярность**: Строка = (дата × nmID WB)  
**Временной диапазон**: 2025-09-01 → сегодня, ~11M строк  

| Поле | Тип | Описание |
|------|-----|----------|
| `[Дата]` | date | Дата |
| `[Артикул WB]` | int | nmID WB (join с `mp.wb_sku.nmID`) |
| `[Заказали товаров, шт]` | int | **Заказы** (момент оформления) ✅ |
| `[Заказали на сумму, руб.]` | float | Сумма заказов (руб.), 100% покрытие ✅ |
| `[Выкупили товаров, шт.]` | int | Выкупы |
| `[Средняя цена, руб.]` | int | NULL на 100% строк — не использовать ⚠️ |
| `[Показы]` | int | Показы карточки |
| `[Положили в корзину, штук]` | int | Добавили в корзину |

**Маппинг nmID → vendorCode**: `mp.wb_sku.nmID` (покрытие 99.8%)

```sql
-- Цена дня + заказы per vendorCode
SELECT [Дата], w.vendorCode,
    SUM([Заказали товаров, шт]) as orders,
    SUM([Заказали на сумму, руб.]) * 1.0 / NULLIF(SUM([Заказали товаров, шт]), 0) as price
FROM [mp].[wb_sales_funnel_lk] f WITH (NOLOCK)
JOIN mp.wb_sku w ON f.[Артикул WB] = w.nmID
WHERE [Дата] >= '2026-02-15' AND [Заказали товаров, шт] > 0
GROUP BY [Дата], w.vendorCode
```

> ✅ Основной источник данных проекта **ELASTICITY_CALC**

---

### mp.ozon_reports
**Назначение**: Транзакции Ozon — начисления, комиссии, эквайринг  
**Join**: `posting_number` → `partner.OzonOrder.PostingNumber`

| Поле | Описание |
|------|---------|
| `operation_id` | PK |
| `posting_number` | Номер отправления |
| `operation_type` | Тип операции (см. ниже) |
| `operation_date` | Дата операции |
| `accruals_for_sale` | Выручка клиент (GMV, с НДС) |
| `sale_commission` | Комиссия Ozon (с НДС, отрицательная) |
| `amount` | Сумма (для эквайринга и других спец. операций) |

**Ключевые типы операций:**
| operation_type | Смысл |
|---------------|-------|
| `OperationAgentDeliveredToCustomer` | Доставка (продажа) |
| `ClientReturnAgentOperation` | Возврат клиента |
| `OperationAgentStornoDeliveredToCustomer` | Сторно доставки |
| `MarketplaceRedistributionOfAcquiringOperation` | Эквайринг |

**Ключевые формулы (подтверждены, Δ<3.5%):**
```sql
-- Кол-во
SUM(CASE WHEN operation_type='OperationAgentDeliveredToCustomer' THEN 1 ELSE -1 END)

-- Выручка без НДС (НДС 22% с 2026-01-01, КРОМЕ детских товаров — 10%)
SUM(accruals_for_sale / (1.0 + CASE WHEN TRY_CAST(o.NDS AS int)=10 THEN 0.10 ELSE 0.22 END))

-- Эквайринг (только для постингов с доставкой)
WHERE operation_type='MarketplaceRedistributionOfAcquiringOperation'
  AND posting_number IN (SELECT posting_number FROM mp.ozon_reports WHERE operation_type='OperationAgentDeliveredToCustomer')
```

> ⚠️ **НДС Ozon с 01.01.2026**: ставка повышена 20%→22%. `OzonOrder.NDS` хранит старое значение 20 — маппить: 20→22, 10→10

---

### mp.ozon_reports_items
**Назначение**: Строки постингов Ozon (детализация по SKU)

| Поле | Описание |
|------|---------|
| `operation_id` | FK → mp.ozon_reports |
| `sku` | Ozon SKU (числовой, join с mp.ozon_sku) |

---

### mp.ozon_sku
**Назначение**: Маппинг Ozon SKU → артикул Кари

| Поле | Описание |
|------|---------|
| `sku` | Ozon SKU (числовой) |
| `article` | Артикул Кари (vendorCode) |
| `FBO_sku` | FBO SKU для join с MPStats |
| `actual` | 1 = актуальный |

---

### mp.ozon_infoPrice
**Назначение**: Информация о текущих ценах Ozon (витрина и продавец)

| Поле | Описание (Ozon API Mapping) |
|------|-----------------------------|
| `Дата обновления` | Время актуализации данных |
| `product_id` | Внутренний ID товара Ozon |
| `offer_id` | Артикул продавца (с суффиксами/размерами) |
| `Артикул` | Очищенный артикул Кари (= `vendorCode`) |
| `Цена до скидки` | Зачеркнутая старая цена (`old_price`) |
| `Цена для покупателя` | **Цена на витрине сайта** (`site_price` / `marketing_price`). Конечная цена для обычного клиента покупателя. |
| `Цена по карте` | Цена со скидкой при оплате Ozon Картой. |
| `Ваша цена` | Цена, установленная продавцом (`price`). |
| `Ваша цена без учета акций` | Базовая цена продавца до применения акций. |

> ✅ Для получения метрики **site_price** (цена, которую видит покупатель на сайте) следует использовать поле `[Цена для покупателя]`.

---

### mp.wb_sku
**Назначение**: Маппинг WB vendorCode → категория WB

| Поле | Описание |
|------|---------|
| `vendorCode` | Артикул Кари |
| `subjectID` | ID категории WB (join с wb_categories) |
| `actual` | 1 = актуальный |

---

### mp.wb_categories
**Назначение**: Справочник категорий WB

| Поле | Описание |
|------|---------|
| `subjectID` | ID категории |
| `subjectName` | Название категории (WB subject) |
| `parentName` | Родительская категория |

```sql
-- Полный маппинг: артикул → WB subject
SELECT UPPER(LTRIM(RTRIM(w.vendorCode))) as sku, c.subjectName, c.parentName
FROM mp.wb_sku w JOIN mp.wb_categories c ON w.subjectID = c.subjectID
WHERE w.actual = 1
```

---

### mp.ozon_marketing
**Назначение**: Расходы на продвижение Ozon

| Поле | Описание |
|------|---------|
| `[Дата]` | Дата расхода |
| `[Расход, с НДС]` | Сумма расхода с НДС |

```sql
-- Промо без НДС
SUM([Расход, с НДС]) / 1.22
```

---

### mp.mpstats_oz_products
**Назначение**: Данные MPStats по товарам Ozon (для маппинга ниши)

| Поле | Описание |
|------|---------|
| `id` | Ozon product ID (=FBO_sku из ozon_sku) |
| `niche` | Ниша MPStats (аналог WB subject) |

---

### mp.ya_orders_transactions
**Назначение**: Транзакции Яндекс Маркет  
**Фильтр продаж**: `Status = 'Доставлен покупателю'`

| Поле | Описание |
|------|---------|
| `[Ваша цена (за шт.)]` | Цена продажи |

---

### mp.ya_vitrina / mp.ya_boost_sale
**Назначение**: Комиссия и реклама Яндекс Маркет (статус: частично изучены)

---

### dbo.GuideAssortiment
**Назначение**: Справочник ассортимента Кари (иерархия, сезон, группа)

| Поле | Описание |
|------|---------|
| `itemid` | Артикул Кари |
| `hierarchy_1_RUS` | Иерархия уровень 1 |
| `hierarchy_2_RUS` | Иерархия уровень 2 |
| `BUSINESSGROUPRU` | Бизнес-группа |
| `KAR_SEASONCODERU` | Сезон |

---

### mp.employers_from_QlikView
**Назначение**: Матрица маппинга товарных групп и брендов на ответственных менеджеров. Данные выгружаются из QlikView. Позволяет привязать ответственное лицо по пересечению "Retail Group" (hierarchy_full) и "Бренда".

| Поле | Описание |
|------|---------|
| `hierarchy_full` | Код розничной группы (например, `797`) |
| `retail_group` | Расшифровка подкатегории (например, `Ботинки мужские зимние для активного отдыха`) |
| `hierarchy1-3` | Уровни иерархии (до `hierarchy3`) |
| `brand` | Бренд товара |
| `manager_name` | ФИО категорийного/ответственного менеджера за группу |
| `group_number` | Номер группы менеджеров |

---


## DBPartners

### dbo.WblmRepForOperMarginReport
**Назначение**: Обогащенная детализация отчета о реализации WB (база для Отчета по маржинальности)  
**Гранулярность**: Как в сыром отчете (каждая продажа/возврат/логистика) + присоединена себестоимость Кари.

| Поле | Описание (из CSV "Детализация") |
|------|---------|
| `REALIZATIONREPORT_ID` | Номер отчета |
| `DATEFROM` `DATETO` | Периоды отчета |
| `DOC_TYPE_NAME` | Тип документа (Продажа, Возврат, Штраф и т.д.) |
| `QUANTITY` | Количество |
| `OFFICE_NAME` | `Склад` — склад WB отгрузки (например, Коледино, Подольск) |
| `PPVZ_OFFICE_NAME` (если есть) | Название ПВЗ (пункта выдачи) назначения / доставки |
| `RETAIL_PRICE` | Цена розничная |
| `PPVZ_FOR_PAY` | К перечислению Продавцу (финальная выручка) |
| `DELIVERY_RUB` | Стоимость логистики |

**Обогащенные (внутренние) поля Кари:**
| Поле | Описание |
|------|---------|
| `ITEMID` | Чистый артикул Кари (`vendorCode`) |
| `COST_WITHOUT_NDS_RUB` | Внутренняя учетная себестоимость (без НДС) |
| `NDS_ESTIMATE` | Расчетная ставка НДС |
| `DOLLAR_RATE` | Курс доллара |

> ✅ **Ключевой вывод**: Для пространственной аналитики логистики и продаж по складам WB следует группировать (GROUP BY) данные по колонке `OFFICE_NAME` (изначально колонка `Склад` в CSV-отчете).

---

### dbo.WblmRepCSB
**Назначение**: Физические отгрузки (Inbounds / Поставки) со складов Кари на склады Wildberries  
**Особенности**: Таблица представляет собой срез (snapshot) текущих активных перемещений на момент обновления (сохранен в колонке `DT`). Это реестр физических поставок, а не реализаций конечным покупателям.

| Поле | Описание |
|------|---------|
| `DT` | Дата и время SQL-среза (snapshot timestamp), а не дата накладной |
| `TRANSFERID` | Номер накладной/перемещения (например, `RU214844471`). Служит ключом для соединения со складскими ERP таблицами (InventTransferJour) |
| `REG_NAME_DESTINATION` | Макрорегион назначения (например, `Мск`, `ЦО`, `Урал`). Показывает долю поставок на конкретный маршрут WB. |
| `AGREGATOR` | Маркетплейс-получатель (обычно `Wildberries`) |
| `ITEMID` | Чистый артикул Кари (`vendorCode`, например `W2008854`) |
| `INVENTSIZEID` | Конкретный размер обуви (например, `38` или `41`) |
| `QTY` | Количество отгруженных штук данного размера |

---

### dbo.WblmRepGetStockWildberries
**Назначение**: Подневные остатки WB на складе маркетплейса

| Поле | Описание |
|------|---------|
| `DT` | Дата снимка |
| `ITEMID` | Артикул Кари (UPPER для надёжности) |
| `QTE` | Доступный остаток (шт) |

**Диапазон**: 622+ дней истории

---

### dbo.WblmRepGetStockOzon
**Назначение**: Подневные остатки Ozon

| Поле | Описание |
|------|---------|
| `DT` | Дата снимка |
| `ITEMID` | Артикул Кари |
| `FREE_TO_SELL_AMOUNT` | Доступный остаток (шт) |

---

### dbo.WblmRepStockType
**Назначение**: Остатки на складах Кари (разбивка по складам и типам хранения)

| Поле | Описание |
|------|---------|
| `Артикул` | Артикул Кари |
| `Размер` | Размер |
| `stock_type` | Тип хранения: Minibox, ВК, мезонин, ФК БОКС, ФК МИКС, ФК МОНО |
| `Соф1, шт` | Склад Соф1 (основной, ~17M мест) |
| `НВСБ, шт` | Склад НВСБ |
| `Краснодар, шт` | Склад Краснодар |
| `RU_АТМ1, шт` | Склад АТМ1 |
| `Иркутск, шт` | Склад Иркутск |
| `Южноурал, шт` | Склад Южноурал |
| `Алматы7, шт` | Склад Алматы7 |

```sql
-- Итого по Кари (все склады)
SELECT Артикул, SUM(
    ISNULL(TRY_CAST([Соф1, шт] AS float), 0) +
    ISNULL(TRY_CAST([НВСБ, шт] AS float), 0) +
    ISNULL(TRY_CAST([Краснодар, шт] AS float), 0) +
    ISNULL(TRY_CAST([RU_АТМ1, шт] AS float), 0) +
    ISNULL(TRY_CAST([Иркутск, шт] AS float), 0) +
    ISNULL(TRY_CAST([Южноурал, шт] AS float), 0) +
    ISNULL(TRY_CAST([Алматы7, шт] AS float), 0)) as kari_stock
FROM dbo.WblmRepStockType WITH (NOLOCK)
GROUP BY Артикул
```

---

### dbo.WblmRepEstimatedMargin
**Назначение**: Себестоимость per-article (скользящее 7-дневное окно!)

| Поле | Описание |
|------|---------|
| `[Артикул]` | Артикул Кари (UPPER) |
| `[Партнер]` | `'Wildberries'` / `'OZON'` / ... |
| `[Дата]` | Дата (⚠️ хранит только последние ~7 дней!) |
| `[Себестоимость]` | Себестоимость с НДС |
| `[Себестоимость без НДС, руб]` | Себестоимость без НДС (приоритет при наличии) |

```sql
-- Себестоимость без НДС (fallback-логика)
SELECT [Артикул],
    CASE WHEN [Себестоимость без НДС, руб] > 0 
         THEN [Себестоимость без НДС, руб]
         ELSE [Себестоимость] / 1.22 END as cost_novat
FROM dbo.WblmRepEstimatedMargin WITH (NOLOCK)
WHERE [Партнер] = 'OZON'  -- или 'Wildberries'
```

> ⚠️ **Покрытие**: WB ~54% артикулов (кешировать!), Ozon ~98.6%  
> ⚠️ **Хранит только ~7 последних дней** — при использовании брать `GROUP BY Артикул + AVG/MAX`

---

## partner (DBPartners или DBReport?)

### partner.OzonOrder
**Назначение**: Заказы Ozon с НДС и себестоимостью

| Поле | Описание |
|------|---------|
| `PostingNumber` | Номер отправления (join с mp.ozon_reports) |
| `ItemId` | Артикул Кари (lowercase!) |
| `NDS` | НДС: 10 или 20 (маппить 20→22 для 2026+) |
| `Cost` | Себестоимость per unit (без НДС) |
| `Qty` | Количество |

> ⚠️ **JOIN rate**: для свежих данных ~71%, для зрелых (>7 дней) ~99.8%

---

## Важные ограничения

| Проблема | Детали |
|---------|--------|
| `WblmRepEstimatedMargin` — 7 дней | Кешировать при регулярных запусках |
| `OzonOrder.NDS` — устаревший | 20→22 с 01.01.2026, маппить вручную |
| WB себестоимость — 54% покрытие | Не все артикулы имеют cost, использовать fallback |
| Логистика/Удержания WB | Не совпадают с QV, причина не установлена |
| `dbo.vWbCost` | Ошибка linked server — недоступен |

---

## Подключение (Python)

```python
import pyodbc
# DBReport
conn_report = pyodbc.connect(
    "Driver={ODBC Driver 17 for SQL Server};"
    "Server=cl01sql;Database=DBReport;Trusted_Connection=yes;"
)
# DBPartners
conn_partners = pyodbc.connect(
    "Driver={ODBC Driver 17 for SQL Server};"
    "Server=cl01sql;Database=DBPartners;Trusted_Connection=yes;"
)
```

---

*Последнее обновление: 2026-03-19. Добавить сюда новые таблицы по мере их обнаружения.*
